description: a ng-6 component-specific stylesheet

uses: css:file

variables:
  schema:
    type: object
    properties:
      component_name:
        type: string
    required:
      - component_name

  form:
    - '*'

definitions:
  file_name: {{component_name}}
