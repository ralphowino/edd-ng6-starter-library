description: an ng-6 component test

uses: javascript:file

variables:
  schema:
    type: object
    properties:
      component_name:
        type: string

    required:
      - component_name

  form:
    - '*'

definitions:
  file_name: {{component_name}}{{'.spec'}}
